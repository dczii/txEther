import React from 'react';
import {
  Platform,
  StyleSheet,
  Text,
  View,
  TextInput,
  TouchableOpacity,
  Modal as RNModal
} from 'react-native';
import { Icon } from 'native-base';
import moment from 'moment'
import _ from 'lodash'


function CoinChecker(props) {
  return (
    <View>
        <Text>Coin Checker View</Text>
    </View>
  );
}

export default CoinChecker

const styles = StyleSheet.create({
   
});
